apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'SubwayScrambler'
            keyPassword 'John1822'
            storeFile file('C:/Users/Johnathan/Documents/AppKeyStore/Subway.jks')
            storePassword 'VQQX9NN1BJ'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "com.logobuico.johnathan.subwaysandwichscrambler"
        minSdkVersion 17
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.facebook.android:facebook-android-sdk:4.0.1'
}
